name: CI

on: 
  push:
    branches: [main]

  workflow-dispatch:

# jobs
jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Add remote
        env:
          HF: ${{ secrets.HF }}
          run: git remote add space https://JayPat78322:$HF@huggingface.co/spaces/JayPat78322/TextSummarization

      - name: Push to hub
        env:
          HF: ${{ secrets.HF }}
          run: git push --force remote add space https://JayPat78322:$HF@huggingface.co/spaces/JayPat78322/TextSummarization main
      - run: echo "🍏 This job's status is ${{ job.status }}."
